#!/usr/bin/env bash

_updated() {
	while :; do
		git fetch -q
		if ! grep status | grep -q 'is up to date'; then
			git pull
		fi
		sleep "${updated[check]:-1800}"
	done
}

{
cd "$mydir"
_updated
} &
